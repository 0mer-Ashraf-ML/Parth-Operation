[pytest]
testpaths = backend/app/tests
markers =
    unit: fast isolated service or helper tests
    integration: API and DB-backed integration tests
    e2e: full business-flow tests across multiple endpoints
